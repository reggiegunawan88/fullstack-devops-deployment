apiVersion: apps/v1
kind: Deployment
metadata:
  name: fullstack-frontend
  labels:
    deployment: fullstack-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: fullstack-frontend
  template:
    metadata:
      labels:
        app: fullstack-frontend
    spec:
      imagePullSecrets:
        - name: ecr-secret
      containers:
        - name: fullstack-frontend
          image: 355446107250.dkr.ecr.us-east-1.amazonaws.com/fullstack-devops/frontend-v1:master-3
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
          env:
            - name: VITE_API_BASE_URL
              value: "http://localhost:3001"
---
apiVersion: v1
kind: Service
metadata:
  name: fullstack-frontend
  labels:
    app: fullstack-frontend-service
spec:
  selector:
    app: fullstack-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
  type: ClusterIP
